# Project map

- Vue 3/Vite frontend in `src/frontend`; Go/Gin backend in `src/backend`.
- Deployment material lives under `deploy/nginx`; real server configs are intentionally ignored by Git.
- Read `mem:deployment` for the local-only HTTPS/ACME boundary and what the redeploy helper does not cover.
- Read `mem:tech_stack` for pinned toolchain; `mem:suggested_commands` for Windows/Linux command locations; `mem:task_completion` for validation commands.